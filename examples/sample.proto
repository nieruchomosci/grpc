syntax = "proto3";

package sample;

message Request {
    string message = 1;
}

message Response {
    string message = 1;
}

service SampleService {
    rpc ask(Request) returns (Response) {
    }
}
